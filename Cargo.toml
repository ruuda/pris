[package]
name    = "pris"
version = "0.0.0"
authors = ["Ruud van Asseldonk <dev@veniogames.com>"]
build   = "build.rs"

[dependencies]
docopt          = "0.6"  # TODO: Upgrade to 0.7 once lalrpop does too.
lalrpop-util    = "0.12"
libc            = "0.2"
rustc-serialize = "0.3"

[build-dependencies]
lalrpop = "0.12"
